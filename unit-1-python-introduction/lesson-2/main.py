"""
Write a simple function that receives an object and returns
the human-readable type of it. The allowed values are:
 * Number
 * String
 * None
 * Other

Example:

human_readable_type("Hello World")  # String
human_readable_type(2)  # Number
human_readable_type(2.5)  # Number
human_readable_type(True)  # Boolean
human_readable_type(None)  # None
human_readable_type(datetime.now())  # Other
"""


def human_readable_type(obj):
    pass
